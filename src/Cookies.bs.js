// Generated by BUCKLESCRIPT, PLEASE EDIT WITH CARE

import * as $$String from "../node_modules/bs-platform/lib/es6/string.js";
import * as Caml_int32 from "../node_modules/bs-platform/lib/es6/caml_int32.js";

function createCookie(name, value, days, param) {
  if (days !== undefined) {
    var date = new Date();
    var offset = date.getTime() + Caml_int32.imul(Caml_int32.imul(Caml_int32.imul(Caml_int32.imul(days, 24), 60), 60), 1000);
    date.setTime(offset);
    var expires = date.toUTCString();
    document.cookie = name + ("=" + (value + ("; expires" + (expires + "; path=/"))));
    return /* () */0;
  } else {
    document.cookie = name + ("=" + (value + "; path=/"));
    return /* () */0;
  }
}

function readCookie(name) {
  var nameEQ = name + "=";
  var cookieChunks = document.cookie.split(";").map($$String.trim);
  var match = cookieChunks.find((function (s) {
          return s.startsWith(nameEQ);
        }));
  if (match !== undefined) {
    return match.substring(nameEQ.length);
  }
  
}

function eraseCookie(name) {
  return createCookie(name, "", -1, /* () */0);
}

function loggedIn(param) {
  var token = readCookie("access_token");
  if (token !== undefined) {
    return token.length > 0;
  } else {
    return false;
  }
}

function logOut(setWarningOpt, param) {
  var setWarning = setWarningOpt !== undefined ? setWarningOpt : false;
  if (setWarning) {
    localStorage.setItem("loginFlash", "Your login has expired or is invalid.");
  }
  return eraseCookie("access_token");
}

export {
  createCookie ,
  readCookie ,
  eraseCookie ,
  loggedIn ,
  logOut ,
  
}
/* No side effect */
